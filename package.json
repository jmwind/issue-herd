{
  "name": "linear-herd",
  "version": "0.1.0",
  "description": "Watch Linear; when an issue matches a rule, open a herdr workspace, run Claude Code in a worktree, brief it, and report the PR back to the issue.",
  "type": "module",
  "bin": { "linear-herd": "bin/linear-herd.mjs" },
  "files": ["bin", "src", "prompts", "config.example.json", "README.md", "LICENSE"],
  "engines": { "node": ">=22" },
  "scripts": {
    "test": "node --test",
    "smoke": "node bin/linear-herd.mjs smoke"
  },
  "keywords": ["linear", "herdr", "claude-code", "agent", "automation"],
  "license": "MIT"
}
