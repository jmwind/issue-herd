# issue-herd: GitHub token — a token with read/write access to the repository's issues (classic scope "repo"), or `gh auth login`.
# Put the real value in .env.local, never here; or skip this and run `issue-herd login`.
GITHUB_TOKEN=
