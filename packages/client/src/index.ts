// @weawr/client — the transport a browser or a phone uses to talk to `weawr serve`.
// Portable: no Node-only imports. Implemented with the versioned CLI interface stage.
export { PROTOCOL_VERSION } from '@weawr/protocol';
